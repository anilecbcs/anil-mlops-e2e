apiVersion: apps/v1
kind: Deployment
metadata:
  name: anil-api-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: anilecbcsa/anil-mlops830api:latest
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: anil-api-svc
spec:
  selector:
    app: api
  ports:
  - port: 8000
    targetPort: 8000
